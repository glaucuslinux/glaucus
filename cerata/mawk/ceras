version=1.3.4-20171017
url=ftp://ftp.invisible-island.net/mawk/mawk-1.3.4-$version.tgz

build_system () {
	tar xfv $CERD/mawk/mawk-$version.tgz -C $TMPD/system/sources
	cd $TMPD/system/sources/mawk-$version

	./configure --prefix=$GLAD \
		CC=$CC \
		CFLAGS="$CFLAGS" \
		LDFLAGS="$LDFLAGS"

	make
	make install
	cd $GLAD/bin
	ln -sfv mawk awk
}
