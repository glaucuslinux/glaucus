#!/bin/execlineb -P
if { mkdir /run/tmp }
if { mount -nt tmpfs -o noatime,nodev,nosuid tmp /run/tmp }
if { mkdir /run/tmp/.X11-unix /run/tmp/.ICE-unix }
if { chmod 1733 /run/tmp/.X11-unix }
chmod 1733 /run/tmp/.ICE-unix
