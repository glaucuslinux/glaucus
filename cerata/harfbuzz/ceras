name=harfbuzz
version=git
release=1
arch=x86_64
url=https://github.com/$name/$name
cyst=musl
description='A text shaping engine'
license='Apachev2.0 GPLv2+ GPLv3+ ISC MIT'
build(){
        rsync -vah $CERD/$name/$name $SSRC --delete
        cd $SSRC/$name
        ./autogen.sh \
                --prefix=/usr \
                --build=$TUPL \
                --host=$TUPL \
                --target=$TUPL \
                --disable-gtk-doc-html
        make
        make DESTDIR=$SCER/$name/sac \
                install-strip
}
