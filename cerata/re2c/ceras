name=re2c
version=git
release=1
arch=x86_64
url=https://github.com/skvadrik/$name
description='Free and open-source lexer generator for C and C++'
license='GPLv3 PD PHPv3.01'
build(){
        rsync -vah $CERD/$name/$name/$name/ $SSRC/$name/ --delete
        cd $SSRC/$name
        autoreconf -vfi
        sed -e 's/-O2//' \
                -i configure
        mkdir -v $SBLD/$name
        cd $SBLD/$name
        $SSRC/$name/configure \
                --prefix= \
                --disable-silent-rules \
                --disable-dependency-tracking
        make
        make \
                install-strip
        make \
                DESTDIR=$SCER/$name/sac \
                install-strip
}
