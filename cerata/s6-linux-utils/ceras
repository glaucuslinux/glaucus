name=s6-linux-utils
version=git
release=1
arch=x86_64
url=https://$version.skarnet.org/cgi-bin/c$version.cgi/$name
cyst=skalibs
description="skarnet's set of minimalistic Linux-specific system utilities"
license=ISC
build(){
        rsync -vah $CERD/$name/$name $SSRC --delete
        cd $SSRC/$name
        ./configure \
                --host=$TRPL \
                --target=$TRPL \
                --build=$TRPL \
                --prefix=/ \
                --libexecdir=/lib/libexec \
                --with-sysdeps=$GLAD/lib/skalibs/sysdeps \
                --with-include=$GLAD/include \
                --with-dynlib=$GLAD/lib \
                --enable-shared \
                --disable-static \
                --disable-allstatic
        sed -e "s/CFLAGS := .*/CFLAGS := $CFLAGS -fno-stack-protector/" \
                -e 's/-Wl,--hash-style=both //' \
                -i config.mak
        make
        make \
                DESTDIR=$SCER/$name/sac \
                install
}
