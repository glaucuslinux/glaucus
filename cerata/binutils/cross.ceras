# Copyright (c) 2018-2020, Firas Khalil Khana
# Distributed under the terms of the ISC License

# Voyager: Firas Khalil Khana (firasuke) <firasuke@glaucuslinux.org>

prepare() {
  $MKDIR $XBLD/$nom &&
  cd $XBLD/$nom
}

configure() {
  $SRCD/$nom/$nom-$ver/configure \
    --target=$TUPL \
    --prefix=$TOOL \
    --enable-deterministic-archives \
    --disable-compressed-debug-sections \
    --enable-new-dtags \
    --enable-relro \
    --enable-default-hash-style=gnu \
    --disable-werror \
    --disable-nls \
    --disable-rpath \
    --disable-initfini-array \
    --with-lib-path=$TOOL/lib \
    --with-sysroot=$GLAD \
    --with-bugurl=https://github.com/glaucuslinux/glaucus/issues \
    --without-included-gettext \
    --with-system-zlib \
    --disable-multilib
}

build() {
  $MAKE \
    configure-host &&

  $MAKE
}

install() {
  $MAKE \
    install-strip &&

  $LN $TUPL-ld.bfd $TOOL/bin/$TUPL-ld &&

  $LN $TOOL/bin/$TUPL-ar $TOOL/$TUPL/bin/ar &&
  $LN $TOOL/bin/$TUPL-as $TOOL/$TUPL/bin/as &&
  $LN $TOOL/bin/$TUPL-ld $TOOL/$TUPL/bin/ld &&
  $LN $TOOL/bin/$TUPL-ld.bfd $TOOL/$TUPL/bin/ld.bfd &&
  $LN $TOOL/bin/$TUPL-nm $TOOL/$TUPL/bin/nm &&
  $LN $TOOL/bin/$TUPL-objcopy $TOOL/$TUPL/bin/objcopy &&
  $LN $TOOL/bin/$TUPL-objdump $TOOL/$TUPL/bin/objdump &&
  $LN $TOOL/bin/$TUPL-ranlib $TOOL/$TUPL/bin/ranlib &&
  $LN $TOOL/bin/$TUPL-readelf $TOOL/$TUPL/bin/readelf &&
  $LN $TOOL/bin/$TUPL-strip $TOOL/$TUPL/bin/strip
}
