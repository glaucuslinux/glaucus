# Copyright (c) 2018-2020, Firas Khalil Khana
# Distributed under the terms of the ISC License

# Voyager: Firas Khalil Khana (firasuke) <firasuke@glaucuslinux.org>

prepare() {
  $RSYNC $SRCD/$nom/$nom-$ver/ $TSRC/$nom --delete &&
  cd $TSRC/$nom &&

  $SCRD/gnu guess . &&
  $SCRD/gnu rpath . &&
  $SCRD/gnu sub . &&

  $MKDIR $TBLD/$nom &&
  cd $TBLD/$nom
} &&

configure() {
  $TSRC/$nom/configure \
    --build=$XBLD \
    --host=$XBLD \
    --target=$XTGT \
    --prefix=$TLCD \
    --libdir=$TLCD/lib \
    --enable-deterministic-archives \
    --disable-compressed-debug-sections \
    --enable-new-dtags \
    --enable-relro \
    --disable-separate-code \
    --enable-default-hash-style=gnu \
    --disable-werror \
    --disable-nls \
    --disable-rpath \
    --disable-initfini-array \
    --with-sysroot=$TLCD/sysroot \
    --with-pkgversion="glaucus $ARCH" \
    --with-bugurl=https://github.com/glaucuslinux/glaucus/issues \
    --without-included-gettext \
    --with-system-zlib \
    --disable-multilib &&

  $MAKE \
    configure-host
} &&

build() {
  $MAKE \
    all-binutils \
    all-gas \
    all-ld
} &&

install() {
  $MAKE \
    install-strip-binutils \
    install-strip-gas \
    install-strip-ld &&

  $LN $XTGT-ld.bfd $TLCD/bin/$XTGT-ld &&

  $LN ../../bin/$XTGT-ar $TLCD/sysroot/bin/ar &&
  $LN ../../bin/$XTGT-as $TLCD/sysroot/bin/as &&
  $LN ../../bin/$XTGT-ld $TLCD/sysroot/bin/ld &&
  $LN ../../bin/$XTGT-ld.bfd $TLCD/sysroot/bin/ld.bfd &&
  $LN ../../bin/$XTGT-nm $TLCD/sysroot/bin/nm &&
  $LN ../../bin/$XTGT-objcopy $TLCD/sysroot/bin/objcopy &&
  $LN ../../bin/$XTGT-objdump $TLCD/sysroot/bin/objdump &&
  $LN ../../bin/$XTGT-ranlib $TLCD/sysroot/bin/ranlib &&
  $LN ../../bin/$XTGT-readelf $TLCD/sysroot/bin/readelf &&
  $LN ../../bin/$XTGT-strip $TLCD/sysroot/bin/strip
}
