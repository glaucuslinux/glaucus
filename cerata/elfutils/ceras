name=elfutils
version=git
release=1
arch=x86_64
url=https://sourceware.org/$version/$name.$version
description='A collection of utilities and libraries to read, create and modify ELF binary files, find and handle DWARF debug data, symbols, thread state and stacktraces for processes and core files on GNU/Linux'
license='GPLv2+ GPLv3+ LGPLv3+'
build_toolchain(){
        :
}
build(){
        :
}
