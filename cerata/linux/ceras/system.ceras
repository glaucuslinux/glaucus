# Copyright (c) 2019, Firas Khalil Khana
# Distributed under the terms of the ISC License

# Voyager: Firas Khalil Khana (firasuke) <firasuke@gmail.com>

prepare_system() {
  rsync -vah $CERD/$name/$name-$version $SSRC/$name --delete

  install -dv $SBLD/$name

  cd $SSRC/$name

  make \
    mrproper

  patch -p1 -i $CERD/$name/patches/uksm-5.4.patch
}

configure_system() {
  rsync -vah $CERD/$name/glaucus.config $SBLD/$name/.config

  make \
    O=$SBLD/$name \
    oldconfig

  make \
    O=$SBLD/$name \
    prepare
}

build_system() {
  make \
    O=$SBLD/$name
}

install_system() {
  install -dv $SCER/$name/sac/boot

  rsync -vah $SBLD/$name/arch/x86/boot/bzImage $SCER/$name/sac/boot/vmlinuz
  rsync -vah $SBLD/$name/System.map $SCER/$name/sac/boot
}
