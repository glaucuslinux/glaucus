# Copyright (c) 2018-2020, Firas Khalil Khana
# Distributed under the terms of the ISC License

$SCRD/check m4 &&
$SCRD/check netbsd-curses &&
$SCRD/check libedit &&
$SCRD/check oksh &&
$SCRD/check dash &&
$SCRD/check libressl &&
$SCRD/check toybox &&
$SCRD/check hostname &&
$SCRD/check which &&
$SCRD/check popt &&
$SCRD/check attr &&
$SCRD/check acl &&
$SCRD/check rsync &&
$SCRD/check diffutils &&
$SCRD/check file &&
$SCRD/check findutils &&
$SCRD/check mawk &&
$SCRD/check grep &&
$SCRD/check make &&
$SCRD/check patch &&
$SCRD/check sed &&
$SCRD/check libarchive
