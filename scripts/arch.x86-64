# Copyright (c) 2018-2020, Firas Khalil Khana
# Distributed under the terms of the ISC License

export ARCH=x86-64 &&

export CARCH=$ARCH &&

export FARCH="-march=$ARCH -mtune=generic -mabi=sysv_abi" &&

export GARCH="$ARCH \
--with-tune=generic \
--with-abi=sysv_abi" &&
export LARCH=x86_64 &&
export MARCH=$LARCH &&

export TUPL=$LARCH-glaucus-linux-musl
