# Copyright (c) 2019, Firas Khalil Khana
# Distributed under the terms of the ISC License

construct() {
  for ceras in $@; do
    . $CERD/$ceras/ceras/ceras
    . $CERD/$name/ceras/cross.ceras
    
    prepare_cross || { echo cross $name prepare failed; exit $?; }
    configure_cross || { echo cross $name configure failed; exit $?; }
    build_cross || { echo cross $name build failed; exit $?; }
    install_cross || { echo cross $name install failed; exit $?; }
  done
}

construct binutils
construct gmp mpfr mpc
construct gcc
