# Copyright (c) 2019, Firas Khalil Khana
# Distributed under the terms of the ISC License

construct() {
  for ceras in $@; do
    . $CERD/$ceras/ceras
    . $CERD/$nom/cross.ceras
    
    prepare_cross || { printf cross $nom prepare failed; exit $?; }
    configure_cross || { printf cross $nom configure failed; exit $?; }
    build_cross || { printf cross $nom build failed; exit $?; }
    install_cross || { printf cross $nom install failed; exit $?; }
  done
}

construct binutils
construct gcc
