# Copyright (c) 2019-2020, Firas Khalil Khana
# Distributed under the terms of the ISC License

$SCRD/check zlib-ng &&
$SCRD/check lbzip2 &&
$SCRD/check zstd &&
$SCRD/check lz4 &&
$SCRD/check libedit &&
$SCRD/check bc &&
$SCRD/check attr &&
$SCRD/check acl &&
$SCRD/check skalibs &&
$SCRD/check utmps &&
$SCRD/check shadow &&
$SCRD/check pkgconf &&
$SCRD/check libcap &&
$SCRD/check psmisc &&
$SCRD/check flex &&
$SCRD/check libtool &&
$SCRD/check gdbm &&
$SCRD/check gperf &&
$SCRD/check expat &&
$SCRD/check hostname &&
$SCRD/check perl-xml-parser &&
$SCRD/check intltool &&
$SCRD/check autoconf &&
$SCRD/check automake &&
$SCRD/check xz &&
$SCRD/check libuargp &&
$SCRD/check musl-fts &&
$SCRD/check musl-obstack &&
$SCRD/check elfutils &&
$SCRD/check libffi &&
$SCRD/check libressl &&
$SCRD/check kmod &&
$SCRD/check python &&
$SCRD/check samurai &&
$SCRD/check meson &&
$SCRD/check grub &&
$SCRD/check pcre2 &&
$SCRD/check less &&
$SCRD/check pigz &&
$SCRD/check iputils &&
$SCRD/check iproute2 &&
$SCRD/check kbd &&
$SCRD/check mandoc &&
$SCRD/check vim &&
$SCRD/check execline &&
$SCRD/check s6 &&
$SCRD/check procps-ng &&
$SCRD/check util-linux &&
$SCRD/check e2fsprogs &&
$SCRD/check s6-linux-init &&
$SCRD/check s6-rc &&
$SCRD/check eudev &&
$SCRD/check s6-boot-scripts &&
$SCRD/check iucode-tool &&
$SCRD/check intel-ucode
